import 'colorful_print.dart';

Future<void> launchLink(String link, [bool openWithActivity = false]) async {
  /* if (!await launchUrl(Uri.parse(link),
      mode: openWithActivity
          ? LaunchMode.externalApplication
          : LaunchMode.platformDefault)) {
    ColorfulPrint.red('Could not launch link: $link');
  } */
}
